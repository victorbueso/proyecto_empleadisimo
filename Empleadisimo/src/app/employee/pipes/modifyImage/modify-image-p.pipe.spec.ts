import { ModifyImagePPipe } from './modify-image-p.pipe';

describe('ModifyImagePPipe', () => {
  it('create an instance', () => {
    const pipe = new ModifyImagePPipe();
    expect(pipe).toBeTruthy();
  });
});
